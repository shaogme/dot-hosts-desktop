{
  # 预设主题名称映射（将模块主题方案名称映射为 Helix 内置主题标识符）
  themePresetMap = {
    "default" = "default";
    "catppuccin-mocha" = "catppuccin_mocha";
    "catppuccin-latte" = "catppuccin_latte";
    "tokyo-night" = "tokyo_night";
    "nord" = "nord";
  };

  # 预设自定义主题配置集合（供扩展与生成 themes/<name>.toml）
  presets = { };
}
